replicas: 1
cpu:
  requests: "25m"
  limits: "100m"
memory:
  requests: "64Mi"
  limits: "2048Mi"
serviceType: LoadBalancer
image:
  Name: tensorflow/tensorflow
  Tag: latest
persistence:
    enabled: true
    accessMode: ReadWriteOnce
    size: 1Gi
